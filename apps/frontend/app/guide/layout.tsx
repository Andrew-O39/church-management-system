import type { ReactNode } from "react";

export const metadata = {
  title: "User Guide · Shepherd",
  description: "How to use Shepherd — handbook for parish members and administrators.",
};

export default function GuideLayout({ children }: { children: ReactNode }) {
  return children;
}
