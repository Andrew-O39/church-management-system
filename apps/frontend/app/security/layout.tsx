import type { ReactNode } from "react";

export const metadata = {
  title: "Security & Data Protection · Shepherd",
  description:
    "How Shepherd protects parish data—access control, separation of records, and responsible practices in simple terms.",
};

export default function SecurityLayout({ children }: { children: ReactNode }) {
  return children;
}
