"""Stable API constants for auth-related HTTP responses (frontend may rely on exact values)."""

# Used with HTTP 403 for inactive accounts (login and authenticated endpoints).
INACTIVE_USER_DETAIL = "Inactive user"
